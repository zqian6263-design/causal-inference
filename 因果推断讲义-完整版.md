# 因果推断讲义：causal-learn 方法体系与实验总览

> **面向读者**：不需要任何编程背景的导师/读者。
> **用途**：把 causal-learn 官网（readthedocs）全部内容与学生的实际实验「整理在一起」——一份可长期查阅、可打印存档的中文体系讲义。
> **配套**：`因果推断方法讲解.md`（15 分钟口头讲稿）、仓库内 9 篇知识库（deep dive）、`results/`（全部图表与指标）。
> **一句话概括**：因果推断 = 从观测数据反推「谁真正导致谁」的因果图；causal-learn 是 CMU CLeaR 官方出品的完整 Python 实现；本文把它有的方法、怎么选、跑出什么结果，按不写代码的方式讲清楚。

---

## 目录

1. [因果推断解决什么问题](#一因果推断解决什么问题)
2. [方法体系总览：三大思路 + 13 个方法](#二方法体系总览三大思路--13-个方法)
3. [方法逐个讲（概念层）](#三方法逐个讲概念层)
4. [什么数据用什么方法（选型决策）](#四什么数据用什么方法选型决策)
5. [实证结果：跑出来的数字说明什么](#五实证结果跑出来的数字说明什么)
6. [对遥感课题的启示](#六对遥感课题的启示)
7. [附录：术语表与仓库地图](#七附录术语表与仓库地图)

---

## 一、因果推断解决什么问题

### 1.1 相关 ≠ 因果

传统数据分析只能回答「两个变量**是否一起变**」（相关）；因果推断回答「一个变量**是否导致**另一个」（因果）。

举遥感领域的例子：
- **相关**：植被指数（NDVI）和作物产量高度相关。
- **因果陷阱**：如果据此说「提高植被指数就能增产」，很可能被坑——因为**土壤湿度同时影响植被指数和产量**，是隐藏的「幕后推手」。你看到的"相关"，其实是「湿度 → 植被」和「湿度 → 产量」两条路径混在一起造成的假象。

> 因果发现要做的，就是把这种「被共同原因混淆」的关系**拆开**，回答：谁真正导致谁。

### 1.2 因果发现给什么

不只是「哪两个变量有关系」，而是一张**带方向的图**：`X → Y` 表示 X 是 Y 的原因。这张图叫**有向无环图（DAG）**——有方向、没有环。

### 1.3 为什么难

- 从同样的数据里，可能有**多张图都说得通**（等价类问题）——有些方向算法**无法仅凭数据确定**，只能给出「无法区分」的折衷表示（CPDAG/PAG）。
- 数据里可能有**没观测到的隐藏变量**在捣乱。
- 不同数据（连续/离散/非线性/时序）需要不同的方法。

**一句话总结本章**：因果推断把「相关≠因果」这个直觉变成能自动跑的算法，输出一张有方向的因果图；难点在于等价类、隐藏变量和数据类型。

---

## 二、方法体系总览：三大思路 + 13 个方法

causal-learn 的搜索方法按「找图思路」分成几大流派，理解流派比记单个方法更重要。

### 2.1 三大主流思路

| 流派 | 核心思路 | 打个比方 | 输出 |
|---|---|---|---|
| **约束型** | 用「条件独立性检验」删边定向：两变量在控制第三者后不再相关，就删掉它们的边 | 排除法：不断做「这个相关是不是别的变量造成的」测试，把假关系一个个剔掉 | CPDAG / PAG |
| **打分型** | 给每个候选图打个分（拟合好 + 复杂度惩罚），搜索分数最高的图 | 评分制：给所有可能的因果图打分，选最优 | CPDAG |
| **函数模型** | 假设变量间的关系有具体数学形式（线性/加性噪声等），利用函数性质定向 | 从「怎么生成的」反推：不同方向会导致可区分的统计特征 | DAG |

**补充流派**：
- **排列型**（GRaSP/BOSS）：把问题转为「找变量的因果顺序」，再还原图。
- **隐变量学习**（GIN/RLCD）：显式找出数据里隐藏的变量。
- **时序**（Granger/VAR-LiNGAM）：专门处理时间序列。

### 2.2 13 个方法一张表

| 方法 | 流派 | 一句话说明 | 适合什么数据 |
|---|---|---|---|
| PC | 约束 | 最常用的通用基线：逐个做条件独立性检验删边定向 | 连续/离散，无隐藏变量 |
| FCI | 约束 | PC 的加强版：**允许隐藏变量**，输出带「不确定」标记的图（PAG） | 怀疑有未观测混杂 |
| CD-NOD | 约束 | 处理**随时间/环境变化的机制**（数据来自不同状态） | 多域/异质数据 |
| GES | 打分 | 贪心加边再删边，找分数最优图 | 连续线性数据的默认 |
| DGES | 打分 | GES 的变体，能处理**确定性关系**（如 X=2Y） | 有函数依赖关系 |
| ExactSearch | 打分 | 暴力搜索**全局最优**（代价：只适用于小图） | ≤15 节点的精确需求 |
| ICA-LiNGAM | 函数 | 假设线性 + 非高斯噪声，恢复**完整方向** | 非高斯连续数据 |
| DirectLiNGAM | 函数 | LiNGAM 的更快版，可加先验 | 同上 |
| VAR-LiNGAM | 函数 | LiNGAM 的时序版 | 时间序列 |
| ANM | 函数 | 加性噪声模型：成对判断方向 | 非线性成对判断 |
| PNL | 函数 | ANM 的一般化（后非线性），更灵活但慢 | 更一般的非线性 |
| GIN / RLCD | 隐变量 | 从观测中**显式还原隐藏变量** | 有潜在未观测变量 |
| GRaSP / BOSS | 排列 | 找变量的因果顺序再还原；**多 seed 必须固定**（内部有随机性） | 通用备选，离散强 |
| Granger | 时序 | 用「过去能否预测未来」判断时序因果 | 多元时间序列 |

> 说明：LiNGAM 家族内部还含 RCD、CAM-UV 两个变体（处理隐藏变量/更一般的函数），因此「13 个方法」是按算法约定计数。

### 2.3 配套模块

方法不是孤立跑的，还要配三样东西：

1. **独立性检验**（约束型的「眼睛」）：fisherz（线性高斯）、chisq/gsq（离散）、kci（非线性、慢）、mv_fisherz（有缺失值）。
2. **打分函数**（打分型的「尺子」）：BIC（线性高斯默认）、BDeu（离散）、CV_general / marginal_general（非线性、很慢）。
3. **评估与图工具**：SHD（两图差距度量）、Confusion（精度/召回）、dag2cpdag/dag2pag（等价类对齐）、可视化。

---

## 三、方法逐个讲（概念层）

> 本章对每个方法只讲三件事：**它解决什么问题 / 大致怎么做 / 什么时候用、什么时候别用**。不涉及代码。

### 3.1 约束型：PC、FCI、CD-NOD

**PC**（Peter-Clark）——最通用的起点。
- 做什么：从零开始，对所有变量对做「条件独立性检验」。如果两个变量在控制某个中间变量后变得独立，就删掉它们之间的边；最后剩下的骨架按规则定向。
- 什么时候用：数据干净（无隐藏变量）、想快速拿一张基线因果图时，它就是首选。
- 限制：只能给出 CPDAG（方向不确定的部分保留无向边）；对隐藏变量敏感。

**FCI**——PC 的「防坑版」。
- 做什么：在 PC 的检验里额外处理「可能存在的隐藏变量」，输出带**圆圈端点**的 PAG 图，明确标出哪些地方不能确定。
- 什么时候用：怀疑数据里有没观测到的共同原因（如气象共因同时影响光谱和产量）。
- 关键点：FCI 的圆圈端点不是失败，而是「诚实的不确定」。

**CD-NOD**——PC 的「时变版」。
- 做什么：数据里混着不同状态的样本（机制切换），把「状态/时间」也当变量参与检验，找出变化发生在哪个变量。
- 什么时候用：多域数据、数据分布随时间/环境改变。

### 3.2 打分型：GES、DGES、ExactSearch

**GES**——连续数据默认。
- 做什么：从一个空图出发，不断加「最能提高分数」的边（前向），再删「去掉后分数更高」的边（后向）。分数 = 拟合好坏 − 复杂度惩罚（BIC）。
- 什么时候用：连续线性数据，与 PC 互为交叉验证。
- 注意：遇到确定性关系（残差方差为 0）时 BIC 会失灵。

**DGES**——专治确定性关系。
- 做什么：先识别「确定性」结构（如 X=2Y），再在保护这些边的前提下打分搜索。
- 什么时候用：特征间有明确的函数依赖。

**ExactSearch**——求全局最优。
- 做什么：不贪心，直接在所有可能图里找分数最高的（用动态规划加速）。
- 什么时候用：节点少（≤15），要绝对最优；节点一多就指数爆炸。

### 3.3 函数模型：LiNGAM 家族、ANM、PNL

这类方法的独特价值：**能给出完整方向（DAG）**，而不只是等价类。

**ICA-LiNGAM**——线性非高斯的「金标准」。
- 做什么：假设 X → Y 是线性关系、噪声是非高斯的。利用非高斯性的统计特征，直接解出因果顺序和方向。
- 什么时候用：数据是连续 + 线性 + 非高斯时，它是**唯一能稳定恢复完整 DAG** 的方法（实测 5 个 seed 全对、SHD=0）。
- 前提检验：噪声必须是**非高斯**的——纯高斯数据下它会失效（这是个经典坑）。

**ANM / PNL**——非线性成对方向判断。
- 做什么：判断「X→Y」还是「Y→X」哪个更符合加性噪声假设（残差与原因独立的程度）。
- 什么时候用：只有一对变量的方向判断，非线性数据。
- 代价：PNL 很慢（导入即 ~45 秒，批量任务要提前导入一次）。

### 3.4 隐变量与排列：GIN、RLCD、GRaSP、BOSS

**GIN / RLCD**——把隐藏变量显式找出来。
- GIN：在「线性非高斯 + 有隐藏变量」的假设下，把被同一隐藏变量影响的观测变量聚类。
- RLCD：输出含 `L1, L2...` 显式隐变量节点的图。
- 什么时候用：明确怀疑有未观测的共因，且想让图里「看得见」隐变量。

**GRaSP / BOSS**——排列思路，离散数据强。
- 做什么：不直接找边，而是先找「变量的因果先后顺序」，再还原图。BOSS 实测在很多数据上表现最稳。
- ⚠️ 内部有随机性：**同一数据跑两次结果可能差很多**（实测 SHD 可从 1 跳到 9）。因此**必须多 seed 跑、报均值±标准差**，单次结果不可信——这是本仓库最重要的纪律之一。

### 3.5 时序：Granger、VAR-LiNGAM

- **Granger**：如果「加入 X 的过去能显著改善对 Y 的预测」，就说 X 格兰杰导致 Y。快，适合筛查。
- **VAR-LiNGAM**：把 LiNGAM 推广到多阶滞后的时间序列，输出更细的滞后结构。

---

## 四、什么数据用什么方法（选型决策）

### 4.1 一张决策树（先看数据特征）

```
拿到数据 → 按顺序问三个问题：

1. 是时间序列？           → 是 → Granger（快速筛查）/ VAR-LiNGAM
2. 怀疑有隐藏变量/混杂？    → 是 → FCI（PAG）或 RLCD（显式隐变量节点）
3. 变量是离散还是连续？
   ├─ 离散 → 小/中规模(≤32 节点) → BOSS+BDeu（实测最优）
   │         大图(≥37 节点)      → PC+chisq（唯一可行，质量会退化）
   └─ 连续 → 关系线性？
        ├─ 线性 + 噪声高斯   → BOSS / PC+fisherz / GES+BIC（BOSS 最稳）
        ├─ 线性 + 噪声非高斯 → ICA-LiNGAM（唯一稳定恢复完整 DAG）
        └─ 非线性           → PC 找骨架 + ANM/PNL 定向（首选）
                              KCI 慢且大样本也不保证恢复（别指望加样本量）
```

### 4.2 速查表（实证结论）

| 你的数据 | 首选 | 备选/交叉验证 | 为什么 |
|---|---|---|---|
| 连续·线性·高斯 | BOSS | PC+fisherz、GES+BIC | BOSS 多 seed 全对（SHD=0.0±0.0） |
| 连续·线性·非高斯 | **ICA-LiNGAM** | BOSS | LiNGAM 唯一稳定完美恢复完整 DAG |
| 离散（小/中） | **BOSS+BDeu** | PC+chisq | 真实 bnlearn 基准全面领先（如 sachs SHD=0 vs 14） |
| 离散（大图 ≥37 节点） | PC+chisq | — | BOSS 的离散打分在图上超时不可行 |
| 非线性 | PC 骨架 + ANM/PNL 定向 | KCI（仅交叉验证） | 线性假设方法全受限；KCI 大样本亦不保证恢复 |
| 时序 | Granger / VAR-LiNGAM | — | 滞后因果 |
| 怀疑隐藏变量 | FCI / RLCD | — | 输出 PAG 或显式隐变量节点 |
| 有缺失值 | PC + mv_fisherz | — | 无需删行 |

### 4.3 三个「别踩」的坑（实测总结）

1. **单 seed 下结论 = 运气**：GRaSP/BOSS 内部随机，单次结果不可信，必须多 seed 报 mean±std。
2. **评估前必须对齐语义**：约束型给 CPDAG、打分型给 CPDAG、函数模型给 DAG——直接拿真值 DAG 和 CPDAG 比，SHD 会虚高。要先把真值图转成同一口径再比。
3. **离散结论随「数据怎么生成的」而变**：分箱得到的离散 → BOSS 最优；真实贝叶斯网络（bnlearn）→ BOSS+BDeu 最优；纯随机构造 → PC+chisq。**任何离散结论都要说明数据来源**。

---

## 五、实证结果：跑出来的数字说明什么

> 本章全部数字来自仓库内真实运行（`results/`），可复现（多 seed 固定 + CI 全绿）。对导师：**只看粗体结论即可**，表格是证据。

### 5.1 方法 × 数据 对比矩阵（合成数据，5 seed 均值±标准差，SHD 越小越好）

| 数据 \ 方法 | PC | GES | GRaSP+BIC | BOSS+BIC | ICA-LiNGAM |
|---|---|---|---|---|---|
| 线性高斯 | 4.8±2.5 | 5.4±3.1 | 6.6±1.4 | **0.0±0.0** ✅ | — |
| 线性非高斯 | 5.6±1.6 | 6.6±1.4 | 5.2±2.9 | 2.4±3.0 | **0.0±0.0** ✅ |
| 非线性 ANM | **4.4±0.5** | 4.8±0.4 | 5.8±1.0 | 5.0±0.0 | — |
| 离散（高斯分箱） | 6.8±1.2 | 6.4±1.0 | 6.0±3.0 | **2.4±2.8** | — |
| 离散（真实 CPD） | **3.2±1.0** ✅ | 5.0±1.3 | 6.0±0.6 | 5.8±0.7 | — |

**结论**：
1. 线性高斯：**只有 BOSS 稳定完美**（5 个 seed 全对）；PC/GES 单次可能对，但平均 4.8–6.6。
2. 线性非高斯：**ICA-LiNGAM 唯一稳定完美**——前提吻合时，它是唯一能给完整方向的。
3. 离散结论**取决于数据怎么生成的**（见 4.3 第 3 条）：真实离散 CPD 下 PC+chisq 反而更好。

### 5.2 样本量敏感性（越大不一定越好）

- **关键发现：大样本不代表更稳**。30000 样本下，PC/GES/GRaSP/BOSS 都是「2/3 的 seed 完美、1 个 seed 全错」（SHD 0 vs 7）——误差主要来自**算法陷入局部最优**，加样本量治不好。
- BOSS 的「舒适区」在 **3000–10000 样本**（稳定完美）；样本量超过后反而退化。
- **ICA-LiNGAM 全样本量稳定**（1000→30000 一律 SHD=0）——唯一不受样本量影响的方法。

### 5.3 真实数据基准（bnlearn 13 个离散网络，10000 样本）

| 结论 | 数值 |
|---|---|
| **小/中规模（≤32 节点）：BOSS+BDeu 全面领先** | 可比 8 集 6 赢 1 平 1 输；sachs（11 节点）**SHD=0 vs PC 的 14** |
| **大图（≥37 节点）：BOSS 打分超时不可行** | alarm（37 节点）>10 分钟不收敛，更大图直接跳过 |
| **大图 PC+chisq 质量退化** | hailfinder SHD=96、精度≈0.18（56 节点，检验误报累积） |

> 图见 `results/figs/bnlearn_{alarm,child,win95pts}_pc_chisq.png`。

### 5.4 官方基准逐位对照（最强可复现证据）

- 用与官方 `Test*.py` **完全一致**的调用重跑，输出矩阵与官方 `benchmark_returned_results` **逐位比对**：
  - **PC+chisq：13/13 一致**（bnlearn 全部数据集）
  - **GES：2/2 一致**（合成 10 节点，BIC 与 BDeu 均零差异）
  - **FCI：5/14 一致**——差异是「版本漂移」（官方基准来自 2022 年旧版本，PAG 定向规则有演化），官方文档自己标注「不一致 ≠ 实现错误」。
- 这意味着：**跑出来的结果和官方库的出厂结果一致**，实验不是"我们自己的口径"，而是对齐了标准。

### 5.5 非线性与 KCI 的诚实结论

- KCI 是非参数检验，理论上最通用，但**实测慢且大样本也不保证恢复**：5 节点 3000 样本要 ~18 分钟，SHD 仍 ≥5，非线性数据甚至出现假阳性。
- **结论：非线性场景别指望加样本量救 KCI**；首选「PC 找骨架 + ANM/PNL 定向」（秒级）。

### 5.6 马尔可夫毯因果特征选择（对遥感最有用的演示）

- 在合成的「特征 X1–X5 + 标签 Y」上（X3 是混淆变量、X2 是纯噪声）：
  - **PC 3 个 seed 全部 SHD=0** 精确恢复因果图；
  - 提取的标签**马尔可夫毯 = {X1, X3}**（真正的因果特征）；
  - **相关筛选被 X4/X5 骗进去**（它们只是经 X3 与 Y 相关、非因果）；
  - **只用 2 个因果特征，分类准确率 = 用全部 5 个特征**（0.935±0.003）。
- **对导师的意义**：因果特征选择用**更少、且语义正确**的特征达到同等效果——这正是「剔除混淆伪相关、提升跨域泛化」的论文价值点。

---

## 六、对遥感课题的启示

1. **因果特征提取是可行的、且有实证支撑**：马尔可夫毯管道（5.6 节）证明了「特征+标签 → 因果图 → 因果特征子集」这条路走得通，且明显优于纯相关筛选。
2. **方法路线建议**：遥感光谱特征通常非线性、可能有共因（如气象），因此主路线建议：
   - **连续数据** → 先 PC 找骨架（或 BOSS），配 GES 交叉验证；非高斯特征可用 LiNGAM 家族定方向；
   - **怀疑有未观测混杂**（气象/环境共因）→ 换 FCI（PAG）；
   - 取标签的**马尔可夫毯**作为特征子集，替代纯相关/纯监督特征选择。
3. **真实数据下一步**：目前定量实验都在合成数据（真值已知才能评估）；真实遥感数据需用 **bootstrap 稳定性检验 + 领域知识核对**（无真值图），再与领域 SOTA 对比。
4. **可复现性资产**：模板（`experiments/10_templates/`）可复制即用，新任务 3 步上手；整套方法/评估/选型决策树都在 `knowledge/08`。

---

## 七、附录：术语表与仓库地图

### 术语表（给非技术读者）

| 术语 | 大白话解释 |
|---|---|
| DAG | 有方向的因果图，无环 |
| CPDAG | 方向没完全确定时的「等价类」表示（有的边是无向的） |
| PAG | FCI 的输出，带「圆圈端点」= 诚实标注不确定 |
| 马尔可夫毯 | 一个变量真正的「原因 + 结果 + 配偶」集合；做特征选择就是取标签的毯 |
| 混淆变量 | 同时影响两个变量的「幕后推手」，制造伪相关 |
| SHD | 两张图差几条边的度量，越小越接近 |
| seed / 多 seed | 算法随机性的种子；多 seed 跑均值±标准差 = 不靠运气下结论 |
| 独立性检验 | 判断「在控制 X 后，A、B 是否还相关」的统计检验 |
| 打分函数 | 给一张候选因果图打分的标准（拟合好坏 − 复杂度惩罚） |

### 仓库地图（代码/数据在哪）

| 想找 | 位置 |
|---|---|
| 9 篇中文知识库（深挖） | `knowledge/00`–`08` |
| 15 分钟口头讲稿 | `因果推断方法讲解.md` |
| 方法对比矩阵 | `results/comparison_report.md` |
| 官方对照 + bnlearn 基准 | `results/metrics/compare_official.json`、`bnlearn_all.json` |
| 全部图 | `results/figs/`（14 张） |
| 每个方法的可运行脚本 | `experiments/01_pc`–`07_granger` |
| 复制即用模板 | `experiments/10_templates/` |
| 遥感因果特征提取演示 | `experiments/demo_remote_sensing/` |

### 如何向导师讲（15 分钟话术要点）

1. **0-1 分钟**：因果推断 = 从数据反推「谁导致谁」；它解决「相关≠因果」（用土壤湿度例子）。
2. **1-9 分钟**：三大思路（约束/打分/函数）+ 一张方法总览表；强调「怎么选」看决策树。
3. **9-13 分钟**：我实际跑了什么 + 结果——重点三句话：① 线性高斯 BOSS 稳定完美、线性非高斯 LiNGAM 完美；② 真实 bnlearn 数据 BOSS+BDeu 领先、大图只能 PC；③ **输出与官方基准逐位一致（PC 13/13、GES 2/2）**，可复现。
4. **13-15 分钟**：对我们的课题——**马尔可夫毯因果特征选择**（2 个因果特征 = 5 个全特征效果），下一步上真实遥感数据。

---

*本讲义由仓库实证数据整理而成 · 全部结论可复现（`git clone` + `pip install` 即可）*
